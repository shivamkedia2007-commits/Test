# Optional: absolute path to the SQLite database.
# DATABASE_PATH=../database/Database.db

# Leave empty for local demo. If set, frontend must send X-API-Key.
API_KEY=
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
PORT=5000
